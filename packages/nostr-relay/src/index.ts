/**
 * NIP-01 NOSTR relay - WebSocket server via Effect v4
 * @eikeon/nostr-relay (core + in-memory)
 *
 * Run with: npm run dev | npm start | nostr-relay
 */

export { inMemoryRelayLayer, program } from "./in-memory-relay.js"
